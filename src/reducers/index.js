import { combineReducers } from "redux";
import Friends from "./friends";

export default combineReducers({
    // app: combineReducers({
    //     friend: Friends,
    //     enem
    // })
    friend: Friends,
});
